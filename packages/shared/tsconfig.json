{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // DOM because `readClientEnv` reads a browser global; node because the tests read files.
    "lib": ["ES2023", "DOM"],
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}

# yaml-language-server: $schema=https://public-assets.zegreatrob.com/dashboard/0.18.0/board-config.schema.json
# This board deliberately has no upstream source or credential. The release reference verifies the
# public package/deployment contract with a supported local visualization.
sources: {}

boards:
  reference:
    refresh: 60s
    panels:
      - id: release-reference
        type: pipeline-animation-demo
        position: { x: 0, y: 0, w: 12, h: 6 }
